# Central (XIAO nRF52840) hardware: PMW3360 trackball on SPI.
CONFIG_SPI=y
CONFIG_INPUT=y
CONFIG_ZMK_POINTING=y
CONFIG_PMW3360=y

# Needed for the &ext_power key: the XIAO has a placeholder ext-power node
# (see tp-sofle_central.overlay); the key is forwarded to the halves.
CONFIG_ZMK_EXT_POWER=y
